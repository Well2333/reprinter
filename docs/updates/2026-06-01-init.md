# 2026-06-01 — 项目初始化

## 变更内容

- 项目立项，完成框架设计与仓库守则制定
- 创建 `docs/REPO-RULES.md`（仓库守则）
- 创建 `docs/README.md`（项目概述）
- 创建 `docs/architecture.md`（技术架构）
- 创建 `docs/updates/2026-06-01-init.md`（本文件）

## 当前状态

**待用户审阅框架文档，确认后开始编写代码。**

## 待确认事项（提交给用户）

1. OFD 处理策略：是否接受"OFD 渲染为 Canvas 截图后嵌入 PDF"的方案？还是要求矢量保留？
2. 发票白边默认值（20mm 上下）是否合适？是否需要用户可调？
3. 输出 PDF 的非发票页面：图片文件是否也需要固定页面尺寸（如 A4），还是按图片实际尺寸生成页面？
4. 是否需要预览功能（生成前在页面内预览合并效果）？
5. 文件排序方式：是否需要拖拽排序，还是只需要上移/下移按钮？

## 影响范围

- 全部（初始化）

## 关联文件

- `docs/REPO-RULES.md`
- `docs/README.md`
- `docs/architecture.md`
