# 2026-06-01 更新记录

## 变更摘要
- 初始化仓库级 git 忽略规则，屏蔽本地环境、构建产物和敏感配置文件。
- 进一步将 `example/` 目录纳入忽略范围，避免个人隐私示例文件被上传。
-- 配置 Vite 的 GitHub Pages 基路径为 `/`（根路径）。
- 增加 GitHub Actions Pages 部署流程。

## 影响范围
- 本地开发环境不会再把 `.venv`、`node_modules`、`dist` 等目录纳入版本控制。
- `example/` 目录继续保留在本地，用于调试，但不会进入远端仓库。
- 构建产物可直接部署到 GitHub Pages 项目站点。

## 关联文件
- `/.gitignore`
- `/vite.config.ts`
- `/.github/workflows/deploy.yml`